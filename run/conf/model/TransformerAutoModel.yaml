name: TransformerAutoModel
params:
  model_name: "/kaggle/input/huggingfacedebertav3variants/deberta-v3-small/config.json" #  推論時はinternet offなので、datasetから取得したconfigを使う
  hidden_size: 120
  stride: ${downsample_rate}
#model_name: "/kaggle/input/v3-small/config.json"
